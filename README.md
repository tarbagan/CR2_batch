# CR2 Batch

Windows desktop application for batch processing `CR2` and other photo files.

## Current scope

- `WPF` desktop app for Windows
- batch queue for files and folders
- Adobe `XMP` preset selection
- `CR2/RAW -> JPG` rendering through Adobe Photoshop automation
- fallback RAW copy + sidecar mode
- raster export and JPG metadata update

## Open in Visual Studio

- Open [PhotoBatchStudio.sln](PhotoBatchStudio.sln)
- Build `PhotoBatchStudio.App`

## Download

If GitHub Pages is enabled for the repository, use the project page:

- `https://tarbagan.github.io/CR2_batch/`

The downloadable Windows build is linked from that page.
